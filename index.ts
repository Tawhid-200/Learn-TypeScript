// Annotating

// Type annotation is used to tell the compiler what type of value a variable holds.

let Name: string = "TypeScript";
let Numbers: number = 5;
let isPublished: boolean = true;

console.log(Name);
console.log(Numbers);
console.log(isPublished);
